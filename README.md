# Steps to new post: (in case I forget, which I always do)
 1. go to `source` branch
 2. create/change "content/post-slug.md"
 3. if any attachments, add them in the `wp-content` directory and list them in the `Attachments:` header.
 4. commit post
 5. make publish
 6. make github